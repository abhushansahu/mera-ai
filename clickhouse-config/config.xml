<?xml version="1.0"?>
<clickhouse>
    <!-- Single node cluster configuration for Langfuse -->
    <remote_servers>
        <default>
            <shard>
                <replica>
                    <host>localhost</host>
                    <port>9000</port>
                </replica>
            </shard>
        </default>
    </remote_servers>

    <!-- Macros for ReplicatedMergeTree tables -->
    <macros>
        <shard>01</shard>
        <replica>replica1</replica>
    </macros>

    <!-- Zookeeper configuration for ReplicatedMergeTree -->
    <zookeeper>
        <node>
            <host>zookeeper</host>
            <port>2181</port>
        </node>
    </zookeeper>

    <!-- Distributed DDL configuration -->
    <distributed_ddl>
        <path>/clickhouse/task_queue/ddl</path>
    </distributed_ddl>
</clickhouse>
